version v1;

User = struct {
    name: string,
    age: enum { age: int, unknown },
    contact: [Contact],
};

Contact = enum {
    phone: int,
    email: string,
    address: struct {
        street: string,
        city: string,
        country: string,
    },
};
